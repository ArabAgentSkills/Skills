# How Skills Work

Skills use progressive disclosure.

- `SKILL.md` is short routing and workflow guidance.
- `vendors/*.md` contains source-backed vendor facts.
- `references/` contains reusable integration or review patterns.
- `scripts/` contains deterministic helpers.
- `evals/` contains prompts and expected behaviors.

Agents should load only the files needed for the user's task.
